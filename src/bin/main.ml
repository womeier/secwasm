let () =
  (*
  TODO: Read input .secwast file -> lex -> parse -> typecheck -> drop labels -> output .wast file

  let oc = open_out "out.wat" in
  Printf.fprintf oc "%s\n" (pp_module example_module);
  close_out oc
  *)
  print_endline
    "Woops! No lexer/ parser exists for the project yet.\n\
     The only meaningful thing to do at the moment is to run the test suite \
     with make test"
